<div align="center">

<img src="../../assets/logo-256.png" width="112" alt="Clockwork">

# Clockwork

**讓電腦上重複性的雜事自動運行**

登入時自動啟動應用程式 · 定時提醒 · 一鍵執行整套流程

**[⬇ 下載 Windows 版](https://github.com/rockbenben/Clockwork/releases/latest)** —— 免安裝，解壓即用

[![365 開源計畫 #020](https://img.shields.io/badge/365%20%E9%96%8B%E6%BA%90%E8%A8%88%E7%95%AB-%23020-1f6feb)](https://github.com/rockbenben/365opensource)

</div>

<div align="center">

[English](../../README.md) · [简体中文](../../README.zh.md) · **繁體中文** · [日本語](README.ja.md) · [한국어](README.ko.md) · [Deutsch](README.de.md) · [Español](README.es.md) · [Français](README.fr.md) · [Italiano](README.it.md) · [Nederlands](README.nl.md) · [Português](README.pt.md) · [Русский](README.ru.md) · [Türkçe](README.tr.md) · [Tiếng Việt](README.vi.md) · [ไทย](README.th.md) · [Bahasa Indonesia](README.id.md) · [हिन्दी](README.hi.md) · [العربية](README.ar.md)

</div>

> 一款 Windows 工作列小工具：啟動啟動器 · 提醒 · 系統開機自啟項 · 動作群組

![Clockwork](../../assets/social-card.png)

一款小巧的 Windows 工作列工具，替你打理每天坐到電腦前那些例行公事：

- 🚀 **啟動清單** — 登入時依序自動開啟你每天要用的應用程式（可為每個步驟設定系統管理員權限、延遲、僅限特定星期 / 僅限某點之前、視窗樣式、若已在執行則啟動、備援路徑），順手還能做幾件雜事（關閉或聚焦視窗、送出按鍵 / 文字、設定音量……）。
- ⏰ **定時任務** — 準時彈出提醒；朗讀出來；按星期 / 每 N 天 / 每月重複；或「登入時」觸發。點擊 **是** 可以執行程式、開啟檔案（例如音樂）或網址，或執行一個動作群組；也支援循環執行（每隔 N 分鐘）與僅一次。
- 🧹 **系統開機自啟項** — 列出**你電腦上所有會自動啟動的項目**，把不需要的關掉（是停用，而非刪除——隨時可再開回來）。一鍵即可「接管」某個項目，把它併入你自己的啟動清單。
- 🎛️ **動作群組** — 把一連串動作打包成一個可重複使用的群組（專注 / 會議 / 收尾 / 就寢……），從工作列、**全域熱鍵**、啟動清單或提醒中一鍵觸發。內建範本可用。

免安裝、完全可攜的單一資料夾、一切都能用滑鼠設定；深色介面、支援高 DPI。

> 📖 **詳細使用說明：** [English](../USAGE.md) · [中文](../USAGE.zh.md)

## 系統需求

- Windows 10 / 11（x64）
- 無需安裝：一個自帶 .NET 執行環境、自包含的單一檔案 `Clockwork.exe`。

## 開始使用

1. 從 [Releases](https://github.com/rockbenben/Clockwork/releases) 下載最新的 `Clockwork-<版本號>.zip`，解壓後裡面是單一個 `Clockwork.exe`；把它丟進任意資料夾（可攜——放哪都行）。想自行建置，請見下方 **給開發者**。
2. 雙擊 **`Clockwork.exe`** 開啟設定視窗。
   - **首次執行**時會在啟動清單與提醒中各載入幾條**範例**，方便你改成自己的——它們預設都沒勾選，你不勾就什麼都不會執行。**動作群組**分頁也自帶兩個可直接執行的群組（離開一下 / 收工·下班）——這兩個**預設已勾選**，因為動作群組不會自己觸發，只有你主動執行它才會跑。你的設定存放在 exe 旁邊的 `clockwork.settings.json`——僅存本機，絕不上傳。
3. 若要每次開機都執行：在 **設定** 分頁點 **登入時啟動**（會以系統管理員權限註冊一個排程工作，因此開機時不會冒出一堆 UAC 提示）。

> 它會安靜地待在工作列。雙擊工作列圖示即可開啟視窗；視窗的關閉按鈕只是把它縮回工作列。要真正結束，請用工作列圖示的右鍵 **結束**。

> **首次執行會有警告，屬正常。** 程式未做程式碼簽章，SmartScreen 會跳出「已保護您的電腦」——點「其他資訊」→「仍要執行」。部分防毒軟體也可能報警，因為寫登錄檔 Run 機碼與排程工作，既是啟動管理器該做的事，也是惡意程式常做的事，從外部無法區分。不想憑信任接受的話，依下方**開發者建置**自行編譯一份，結果一樣、執行檔是你自己的。

## 螢幕截圖

![Screenshot](../../assets/screenshot.png)

## 五個分頁

五個標籤頁，每個欄位的細節都寫在[完整使用說明](../USAGE.zh.md)裡。

- **啟動清單** —— 開機從上到下依序執行。類型：啟動程式 · 傳送按鍵 · 傳送文字 · 音量 · 視窗動作 · 系統命令 · 動作群組 · 延遲 · 訊息。每步可設「執行後延遲」「重複次數」與「僅某些星期 / 僅 N 點前」；啟動程式還能設系統管理員、視窗樣式、已在執行則啟動、備用路徑。
- **定時任務** —— 時間（或「登入時」）× 週期（星期 / 每 N 天 / 每月 / 僅一次）× 一種動作：彈出提醒（帶「稍後」的是 / 否視窗，或角落卡片，可朗讀）或靜默執行一個動作群組。另有間隔執行、重複催促、錯過補觸發、系統匣勿擾。
- **系統開機自啟項** —— 列出電腦裡所有開機自動啟動的項目（登錄檔 Run 機碼、啟動資料夾、排程工作）：可關掉（只停用、不刪除）、接管到自己的啟動清單，或徹底刪除。
- **動作群組** —— 把一串動作打包重複使用，可從系統匣、**全域熱鍵**（再按一次取消這次執行）、啟動清單步驟或定時任務觸發。群組能整組重複、能引用別的群組（儲存時擋下環狀引用）；「訊息」步驟用是 / 否為後續步驟把關。
- **設定** —— 啟動延遲（0–600 秒，僅開機時）、啟動時最小化到系統匣、開機自動啟動、緊急停止熱鍵、介面語言（18 種）、匯出 / 匯入設定。

> **隨時叫停** —— 標籤列右端的**緊急停止按鈕**（只在有東西在執行時出現）、系統匣「停止正在執行的動作」，或全域**緊急停止熱鍵**（預設 `Ctrl+Alt+Q`）。啟動延遲、等視窗出現這類長等待會被立即中斷。

## 小技巧

- **雙擊某一列即可編輯**。填寫路徑 / 行程 / 快速鍵 / 日期時不必手動輸入：有 **瀏覽…**、**挑選…**（可搜尋的行程挑選器）、**擷取** 與 **挑選日期**。
- **拖曳某一列即可調整順序**：啟動清單、定時任務、動作群組三個清單，以及群組編輯器的步驟清單都支援（上移 / 下移按鈕照樣能用）。
- **儲存前先試跑一遍** —— 群組編輯器裡有 **▶ 執行這一步** 與 **▶ 執行整組**，跑的都是畫面上此刻的內容。整組執行時按鈕會變成 **■ 停止**，關掉編輯器也會讓它停下來。
- **複製**（定時任務 / 動作群組分頁）會把選取的那一列複製一份到它下方——比從頭重建一個幾乎一樣的快多了；複製出來的群組會命名為「…（副本）」。
- **刪除一律會先詢問**，處處如此——清單列、群組編輯器裡的步驟，以及系統開機自啟項。
- 雙擊 `Clockwork.exe` 只會開啟設定——它**不會**立刻執行啟動清單；要執行請用工作列的 **重新執行啟動清單**。
- **以正常方式啟動它**（雙擊 / 工作列 / 排程工作）。某些沙箱 / 降權啟動器會封鎖低階呼叫，因此送出按鍵 / 視窗動作 / 若已在執行則啟動 / 送文字到行程 / 音量可能無法運作（你會收到明確的提示；單純的「啟動程式」不受影響）。
- 你的設定是 `clockwork.settings.json`（僅存本機）。刪掉它即可重設回範例。任務狀態是 `clockwork.state.json`（也是本機；可安全刪除）。
- 新增 `.ahk` 步驟需要已安裝 AutoHotkey。全域熱鍵 / 文字展開不在本工具範圍內——那是 AutoHotkey 的強項。

## 給開發者

C#/.NET WPF；原始碼在 `app/`（需要 .NET 10 SDK）。分層：`Core/` 純邏輯 · `Native/` Win32 互通 · `Engine/` 執行 · `ViewModels/` + `Views/` UI · `I18n/` + `Resources/` 在地化（中性 = 中文原文，每種語言一個 `Strings.<code>.resx` 附屬檔）。

- 執行測試（xUnit）：
  ```powershell
  dotnet test app.Tests/Clockwork.Tests.csproj
  ```
- 建置自包含的單一檔案 exe（單一檔案 / 自包含 / 壓縮已在 csproj 中設定）：
  ```powershell
  dotnet publish app/Clockwork.csproj -c Release -r win-x64
  ```
  輸出：`app/bin/Release/net10.0-windows/win-x64/publish/Clockwork.exe`。
- **CI / 發行**（GitHub Actions）：push / PR 會在 Windows runner 上建置並執行所有測試；推送 `v*` 標籤（例如 `v2.0.0`）會建置、依標籤標記檔案版本、建立 GitHub Release 並附上 `Clockwork-<標籤>.zip`（內含 `Clockwork.exe`）。

## 關於 365 開源計劃

[365 開源計劃](https://github.com/rockbenben/365opensource) 的第 **#020** 個專案——一人 + AI，一年 300+ 個開源專案。

[提交你的點子 →](https://365.aishort.top/) · [Discord](https://discord.gg/PZTQfJ4GjX) · [Telegram](https://t.me/aishort_top)