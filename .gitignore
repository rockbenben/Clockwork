# 本机运行时生成的个人配置（含个人路径，不入库）。示例见 clockwork.settings.example.json
clockwork.settings.json

# 临时 / 系统 / 工具
*.log
Thumbs.db
desktop.ini
.claude/
.vs/
.temp/   # 兜底 TEMP：仅当系统 %TEMP% 全部不可写时，才在应用目录下创建（极少见）

# SDD 工作目录（briefs / reports / review diffs / 进度账本）
docs/

# C#/.NET 工程（主分支 main）的构建产物 —— 同一工作区切分支时会残留在这里。
# 本 PowerShell 归档分支一并忽略，免得误提交，也不依赖机器全局 gitignore；这些目录 main 也忽略，故不与主分支冲突。
bin/
obj/
publish/
app/bin/
app/obj/
app.Tests/bin/
app.Tests/obj/
