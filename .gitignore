# 本机运行时生成的个人配置（含个人路径，不入库）。示例见 clockwork.settings.example.json
clockwork.settings.json

# 发布输出现在由 pubxml 的 PublishDir 决定，落在 app/bin/ 下（已被 app/.gitignore 忽略）。
# 下面两条是历史遗留：早先 CI 用 `dotnet publish -o publish`，照着跑过的机器仓库根还留着这些目录。
publish/
publish-needs-dotnet10/

# 测试结果：MTP 跑失败时会在 bin 下写日志；这条留给任何显式传 --results-directory 的本地调用
TestResults/

# 临时 / 系统 / 工具
*.log
Thumbs.db
desktop.ini
.claude/
# AI 助手的本地工作目录：跨会话记忆（memory/*.md）、计划草稿等。
# 同 .claude/ 的口径——它是**这台机器上这个工作副本**的上下文，不是项目的一部分：
# 里面会写进本机绝对路径、临时诊断结论、还没定下来的取舍，换个开发者就没有意义了。
.workbuddy-ai/
.vs/
# 兜底 TEMP：仅当系统 %TEMP% 全部不可写时，才在应用目录下创建（极少见）
.temp/

# SDD 内部工作目录（planning/specs，本地生成、不入库）；docs/ 其余用于真实文档
docs/superpowers/
# SDD 执行期的账本 / 简报 / 审查包（纯本地草稿，随计划完成即删）
.superpowers/

# 第三方资料的本地副本（竞品手册等），仅供参考，不入库
docs/research/

# Python 字节码缓存（tools/ 下的生成脚本会留下它）。
# 不只是「不该入库的产物」：.pyc 里嵌着编译那台机器上源文件的**绝对路径**，
# 提交它等于把作者的本机目录结构写进版本库。实测已经进去过一次
# （tools/__pycache__/make_icon.cpython-314.pyc 里就是一条 D:\Backup\... 的完整路径）。
__pycache__/
*.pyc
