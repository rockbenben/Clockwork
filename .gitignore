# 本机运行时生成的个人配置（含个人路径，不入库）。示例见 开机助手-设置.example.json
开机助手-设置.json

# 临时 / 系统 / 工具
*.log
Thumbs.db
desktop.ini
.claude/
.vs/
.temp/   # 兜底 TEMP：仅当系统 %TEMP% 全部不可写时，才在应用目录下创建（极少见）

# SDD 工作目录（briefs / reports / review diffs / 进度账本）
docs/
